# pnpm configuration for Vercel deployment
# -------------------------------------------------
# Allow all dependency build scripts to run.
# This is needed for packages like esbuild that require
# a native binary compilation step (node-gyp / postinstall).
# Without this, Vercel's pnpm install silently skips the
# build scripts and the binary is missing at runtime.
enable-pre-post-scripts=true
