import type { Metadata } from "next";
export const metadata: Metadata = { title: "File Manager" };
export default function FilesPage() {
  return (
    <main style={{ padding: "2rem", color: "var(--text-base)" }}>File Manager — Step 5</main>
  );
}
