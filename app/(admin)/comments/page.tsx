import type { Metadata } from "next";
export const metadata: Metadata = { title: "Comments" };
export default function CommentsPage() {
  return <main style={{ padding: "2rem", color: "var(--text-base)" }}>Comments — Step 5</main>;
}
