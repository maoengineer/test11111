import type { Metadata } from "next";
export const metadata: Metadata = { title: "Analytics" };
export default function AnalyticsPage() {
  return <main style={{ padding: "2rem", color: "var(--text-base)" }}>Analytics — Step 5</main>;
}
