import type { Metadata } from "next";
export const metadata: Metadata = { title: "Users" };
export default function UsersPage() {
  return <main style={{ padding: "2rem", color: "var(--text-base)" }}>Users — Step 5</main>;
}
