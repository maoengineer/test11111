import type { Metadata } from "next";
export const metadata: Metadata = { title: "Settings" };
export default function SettingsPage() {
  return <main style={{ padding: "2rem", color: "var(--text-base)" }}>Settings — Step 5</main>;
}
