import type { Metadata } from "next";
export const metadata: Metadata = { title: "Dashboard" };
export default function DashboardPage() {
  return (
    <main style={{ padding: "2rem", color: "var(--text-base)" }}>Admin Dashboard — Step 5</main>
  );
}
