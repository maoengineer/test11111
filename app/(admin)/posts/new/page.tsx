import type { Metadata } from "next";
export const metadata: Metadata = { title: "New Post" };
export default function NewPostPage() {
  return (
    <main style={{ padding: "2rem", color: "var(--text-base)" }}>New Post — Step 5</main>
  );
}
