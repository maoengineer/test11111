import type { Metadata } from "next";
export const metadata: Metadata = { title: "Posts" };
export default function PostsPage() {
  return (
    <main style={{ padding: "2rem", color: "var(--text-base)" }}>Posts Manager — Step 5</main>
  );
}
