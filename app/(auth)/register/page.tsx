import type { Metadata } from "next";
export const metadata: Metadata = { title: "Register" };
export default function RegisterPage() {
  return <main style={{ padding: "2rem", color: "var(--text-base)" }}>Register — Step 3</main>;
}
