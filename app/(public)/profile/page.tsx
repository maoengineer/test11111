import type { Metadata } from "next";
export const metadata: Metadata = { title: "Profile" };
export default function ProfilePage() {
  return <main style={{ padding: "2rem", color: "var(--text-base)" }}>Profile — Step 6</main>;
}
