import type { Metadata } from "next";
export const metadata: Metadata = { title: "Search" };
export default function SearchPage() {
  return <main style={{ padding: "2rem", color: "var(--text-base)" }}>Search — Step 6</main>;
}
